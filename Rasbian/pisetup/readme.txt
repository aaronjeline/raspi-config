--Aaron Eline

Script for basic setup of raspberry pi

Will update/upgrade packages, and install zip, tmux, vim

Will set up a .bashrc that is a copy of pibash

sshd_config will be copied to /etc/ssh/sshd_config
